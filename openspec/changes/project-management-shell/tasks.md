# Tareas: project-management-shell

- [x] Shell `MainShellPage` con drawer, `IndexedStack` y secciones Inicio / Proyectos / Comunidad / Ajustes (pie).
- [x] Selector de idioma en pie del drawer (`LanguageFlagSelector` expandible, sin fondo oscuro).
- [x] Modelo `LiteraryWork` y sección Proyectos con CRUD en memoria (`LiteraryWorksSection`).
- [x] `LiteraryWorkEditorPage`: formulario pantalla completa, Markdown + vista previa, tags, idioma de obra.
- [x] i18n: pestaña «Comunidad» (antes Biblioteca), cadenas de proyectos y shell.
- [x] Dependencia `flutter_markdown`; pruebas y `flutter analyze` en verde.
- [x] (Rama experimental) `flutter_quill`, `markdown_quill`, `markdown`; editor con toolbar; `FlutterQuillLocalizations`; delta inicial no vacío.
- [x] Actualizar `openspec/specs/product/spec.md` y `openspec/specs/client/spec.md` con el comportamiento anterior.
- [x] Crear cambio OpenSpec `project-management-shell` (propuesta, diseño, deltas, tareas).
